name: Update Flake
on:
  workflow_dispatch: # Umożliwia ręczne kliknięcie w panelu
jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: DeterminateSystems/nix-installer-action@v12
      - uses: DeterminateSystems/update-flake-lock@v23
